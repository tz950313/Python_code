#!usr/bin/env python
#-*- coding:utf-8 -*-
s = 1
l = 6
from random import choice
import string
import getpass
def GenPassword(length=8,chars=string.digits+string.digits):
    return ''.join([choice(chars) for i in range(length)])
if __name__=="__main__":
	for i in range(int(s)):
		x = []
		n = (GenPassword(int(l)))
		y = x.append(n)
		print "Random Password",":",n
		
name = raw_input("Username:")
Password = raw_input('Password:')
#当账户密码输入错误时，先循环执行name，再执行Password

while  Password !=str(n) or name != "tz950313" : 
	print(" Please input the correct username password ")
	name = raw_input("Username:")	
	Password = raw_input('Password:') 

import time
i = 5
i=i-1

while i!='1' and i>1:
	time.sleep(1)
	i=i-1
	print ('>>>'),i
	
time.sleep(1)

print("success login")

#raw_input("\n\npress the enter key to exit.")
